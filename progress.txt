# Progress Log

This file tracks development history across agent sessions.
Each entry documents the documentation gate, what was done, how it was tested,
and any relevant notes.

---

## [2026-05-05] - Overlay Height Config Safety Regression

### Documentation Gate:
- User reported the recently changed expanded overlay height is incorrect after commits `d0d2e7750fbfefc2f920ad3ff47c8bdec7679cdb` and `ec83c47144a33de45d9a92ddcfca008bfefd38f4`.
- Investigation found `ec83c47` moved overlay dimensions into the config system but left ordinary expanded minimum height able to resolve to `180` instead of the required usable floor, and approval focus height still depended on the generic expanded max height instead of a dedicated `600x720` focus config.
- `d0d2e77` added Bash command analysis to approval content, increasing approval body height and making the missing focus-layout safety more visible.
- This is an implementation bug in the config-backed layout defaults and normalization, not a rollback of the config system.

### Planned correction:
- Keep overlay dimensions in the config system.
- Add explicit approval focus width/height config fields.
- Normalize config-backed layout values so user or migrated config can widen/tallify the overlay but cannot shrink ordinary expanded mode below 400px or approval focus below 600x720.
- Keep approval footer visible and long approval content scrolling only in `.approval-panel__body`.

### Documentation:
- Requirement ref: `DESIGN.md#Overlay.tsx`
- Design ref: `docs/animation-design.md#3.3 明显弹性灵动岛尺寸`
- Docs updated: yes

---

## Project Status

- **Started:** 2026-04-25
- **Current Phase:** Phase 2 - Real Integration
- **Completed Tasks:** 40 / 41 (plus 1 removed)
- **Active Task:** None

---

## [2026-05-04] - Bash Approval Command Analysis Fix

### Documentation Gate:
- User reported Bash approval requests did not show command-line parsing in the approval UI.
- Existing backend PermissionRequest already emits `tool_input`, but existing frontend docs did not define approval-stage command analysis.
- Updated `DESIGN.md` before source edits to define Bash PermissionRequest command analysis in the approval panel while preserving history-detail analysis.

### Planned correction:
- Preserve approval state semantics: approval requests remain `approval`, not `thinking` or `running`.
- Store PermissionRequest `tool_input` on the frontend approval request.
- Render command analysis in both the approval panel and tool execution history details.

### What was done:
- Added shared Bash command extraction helper for `command` / `cmd` / `script`.
- Added `toolInput` to `ApprovalRequest` and wired `permission_request.tool_input` into it.
- Rendered `CommandAnalysis` inside Bash permission approvals before diff content.
- Kept `ToolExecutionDetail` command analysis by reusing the shared helper.
- Fixed `test-bridge.ts` missing `ToolExecution` type import and propagated simulated approval `tool_input`.

### Testing:
- `frontend`: `npm run build` passed.
- `frontend`: `npx vitest run src/__tests__/components/ApprovalPanel.test.tsx --pool=threads` passed (10 tests).
- `frontend`: `npx vitest run src/__tests__/hooks/useAgentEvents.test.ts --pool=threads` passed (17 tests).
- `frontend`: default `npm run test -- ApprovalPanel.test.tsx useAgentEvents.test.ts` hit a Vitest fork worker startup timeout on Windows; rerun with `--pool=threads` passed.
- `src-tauri`: `CARGO_TARGET_DIR=target-codex-command-analyzer cargo test command_analyzer -- --nocapture` passed (26 command analyzer tests).

---

## [2026-05-04] - Tauri Geometry Sandbox Verification Page

### Documentation Gate:
- User requested a minimal in-app verification page for the browser-vs-Tauri size mismatch around adaptive height, fixed approval focus height, internal scrolling, and footer button visibility.
- Existing docs already require approval/question/plan focus mode to use fixed `600x720`, but did not explicitly define that action buttons are outside the scroll body and must remain visible when the body overflows.
- Updated `DESIGN.md` and `docs/animation-design.md` before source edits to define the header/body/footer contract and the development-only `?sandbox=geometry` verification page.
- Follow-up implementation bug: user verified in Tauri/WebView2 that footer buttons were not actually always visible. Adaptive Short clipped the footer at the end of animation, and Adaptive Long/fixed modes showed only part of the footer.
- Expected behavior remains unchanged: footer is outside the scrolling body and must remain fully inside the overlay/window bounds.
- Follow-up root cause from Tauri screenshot: on the user's 125% DPI display the native window stayed `600x720` physical pixels while WebView content still laid out at `600x720` CSS pixels, so the effective CSS viewport was too small and clipped the right/bottom edges. The resize path must use the effective WebView scale factor, not only `GetDpiForWindow` when that returns `1.0`.
- Follow-up screenshot review corrected the diagnosis: native size was already `750x900 @1.25`, but the CSS viewport was only about `473x567` because WebView reported `devicePixelRatio ~= 1.5875`. The resize path must use the WebView-reported DPR when it is larger than native DPI.

### Planned correction:
- Add a development-only geometry sandbox route without changing the main Overlay business flow.
- Reuse the real `AnimatedOverlay` and `update_overlay_size` path so Tauri/WebView2 native size behavior is tested, not just browser DOM layout.
- Show DOM, scroll, footer visibility, page root scroll, runtime, and native window geometry diagnostics.

### What was done:
- Added `GeometrySandbox` and `GeometrySandbox.css` under `frontend/src/components`.
- Added development-only `?sandbox=geometry` and `VITE_GEOMETRY_SANDBOX=true` entries in `App.tsx`; `Ctrl+Shift+G` remains a browser/dev WebView helper only when the WebView has keyboard focus. The normal Overlay path is unchanged.
- The sandbox includes compact, adaptive short, adaptive long, approval fixed, question fixed, and plan fixed modes.
- Fixed-mode sandbox panels use header/body/footer layout where the footer buttons stay outside the body scroll area.
- Diagnostics show runtime, viewport, root scroll position, overlay/panel/body/footer rects, body scroll metrics, footer visibility, and Tauri `get_window_geometry` when available.
- Added startup WebView zoom reset to `1.0`, passed `window.devicePixelRatio` through `update_overlay_size`, and expanded geometry diagnostics with DPR, visual viewport scale, browser outer size, and native inner/outer size.
- Reduced the geometry sandbox adaptive floor so short-content verification follows measured content height instead of leaving an artificial blank shell area.

### Testing:
- `frontend`: `npm run build` passed.
- `src-tauri`: `cargo check` passed.
- Browser/CDP against `http://localhost:5187/?sandbox=geometry` passed:
  - Adaptive Long: overlay `600x720`, footer visible, body `scrollHeight 1066 > clientHeight 444`, body scrollTop changed `0 -> 160`, root scrollTop `0`.
  - Approval Fixed: overlay `600x720`, footer visible, body `scrollHeight 1221 > clientHeight 444`, body scrollTop changed `0 -> 160`, root scrollTop `0`.
  - Question Fixed: overlay `600x720`, footer visible, body `scrollHeight 1109 > clientHeight 444`, body scrollTop changed `0 -> 160`, root scrollTop `0`.
  - Plan Fixed: overlay `600x720`, footer visible, body `scrollHeight 1210 > clientHeight 444`, body scrollTop changed `0 -> 160`, root scrollTop `0`.
- Tauri native automation against the already-running overlay could not enter the sandbox with `Ctrl+Shift+G`; the window stayed compact `236x52`, consistent with the overlay's `NOACTIVATE` behavior. Tauri sandbox verification should start the dev app with `VITE_GEOMETRY_SANDBOX=true`.
- Restarted the Vite dev server with `VITE_GEOMETRY_SANDBOX=true` and launched `cargo tauri dev`; native Win32 rect checks passed:
  - Initial geometry sandbox window: `600x720`.
  - After clicking Approval Fixed: `600x720`.
  - After clicking Question Fixed: `600x720`.
  - After clicking Plan Fixed: `600x720`.

### Follow-up:
- Manual Tauri/WebView2 verification should open the app with `VITE_GEOMETRY_SANDBOX=true`, then confirm fixed modes keep footer buttons visible while only the body scrolls.

---

## [2026-05-04] - Tauri Approval Focus Fixed Height Recovery

### Documentation Gate:
- User clarified browser automation is normal, but the Tauri Windows app still cannot scroll approval/question/plan focus panels and action buttons are hidden by insufficient height.
- Recent local changes made approval focus height depend on hidden DOM measurement and adaptive resize, which does not reliably match the native WebView window size in Tauri.
- Existing design intent is a single internal approval/question/plan scroll area with no outer page scrollbar; for Tauri reliability this is treated as an implementation bug in the adaptive approval focus height path.

### Planned correction:
- Restore approval/question/plan focus mode to fixed `600x720`.
- Keep normal non-approval expanded panels on adaptive height.
- Keep the top capsule able to temporarily collapse and reopen the pending approval without submitting a response.
- Keep `.approval-panel__body` as the only scrolling viewport for long permission, question, and plan content.

### What was done:
- Removed the hidden approval measurement render path from `Overlay.tsx`.
- Restored approval/question/plan focus height to `720` while preserving the 600px expanded width from config.
- Tightened approval panel flex sizing so header/footer stay fixed and the body receives the constrained scroll area.
- Updated `DESIGN.md` and `docs/animation-design.md` to describe fixed `600x720` focus mode instead of adaptive approval focus height.

### Testing:
- `frontend`: `npm run build` passed.
- Tauri Windows app manual verification still required because the regression only reproduces in the native WebView window, not browser automation.

### Follow-up:
- User reported the fixed-height recovery still left content partially hidden and created large blank areas in all three panels.
- Adjusted approval panel sizing from fill-first to content-first: the body no longer grows to consume all empty space, but still shrinks and scrolls when long content exceeds the available focus height.
- Added explicit approval focus size retries from `Overlay.tsx` so the Tauri WebView receives final `600x720` syncs even if an animation-frame resize is throttled or stale.

---

## [2026-05-03] - Approval Focus Scroll and Manual Collapse Follow-up

### Documentation Gate:
- User reported the latest approval UI cannot scroll and the top capsule cannot collapse the overlay when they do not want to approve immediately.
- Existing `DESIGN.md` and `docs/animation-design.md` already define a single internal approval/question scroll area, no outer Overlay scrollbar, and a clickable compact capsule.
- This is an implementation bug in the recent approval focus handling: approval focus currently forces expanded state while a request exists, and the approval body scrollbar was hidden.

### Planned correction:
- Keep approval/question/plan requests auto-expanding when they arrive.
- Allow the top bar to temporarily collapse a pending approval into the compact capsule without submitting a response.
- Let clicking the compact capsule reopen the same pending approval.
- Restore a visible styled scrollbar on the approval panel body while keeping root/page scrollbars hidden.

### What was done:
- Added per-request approval focus collapse state in `Overlay.tsx`.
- The top bar now collapses/reopens the current pending approval instead of being overridden by forced approval focus expansion.
- Preserved auto-expansion for new approval/question/plan requests.
- Restored the approval panel body as the single visible internal scroll area and constrained the panel to the available focus layout height.
- Follow-up after user confirmed scrolling still does not move: route wheel events from the whole approval card into the body scroller and tighten the flex height constraints so the body has a real bounded viewport.
- Follow-up after browser automation and user video showed scrollbar jitter: approval focus height measurement now uses a hidden 600px-wide measurement copy before expansion. New approval requests do not animate until the final height is known; normal non-approval panels keep ResizeObserver-based adaptive height.
- `AnimatedOverlay` final size sync now reads the latest dimensions, preventing an older animation completion callback from resyncing stale height.
- Follow-up after Tauri/WebView still could not scroll: split measurement and live flex behavior. The hidden measurement copy keeps natural content height, while the visible approval focus fills the computed overlay height and lets `.approval-panel__body` shrink into the only scroll viewport.

### Testing:
- `frontend`: `npm run build` passed.
- `frontend`: `npm run test -- ApprovalPanel.test.tsx` passed.
- Browser automation against `http://localhost:5187` passed:
  - Short diff approval: overlay height `424`, footer visible, no measurement copy left mounted.
  - Long approval: overlay height `720`, footer visible, body `scrollHeight 697 > clientHeight 485`.
  - Wheel scroll changed body `scrollTop` from `0` to `212`.

---

## [2026-05-03] - Approval Focus Outer Scrollbar Follow-up

### Documentation Gate:
- User reported the latest approval focus layout still renders too short in practice, hiding sessions/approval content.
- User clarified the visible white scrollbar looks like an outer Windows/WebView-native page scrollbar, not the intended internal content scroller.
- User then confirmed logs still show `update_overlay_size: CSS(236x52)`, proving the frontend can still sync the compact geometry while an approval-focused render is expected.
- User provided a later trace stabilizing at `CSS(420x190)`, proving the UI can still fall back to normal adaptive expanded mode instead of approval focus.
- Existing `DESIGN.md` already requires approval/question focus mode to use 600x720 and avoid extra outer Overlay scrollbars, so this is an implementation bug.

### Planned correction:
- Lock `html`, `body`, and `#root` to the WebView viewport so the page itself cannot scroll.
- Hide root-level native scrollbars.
- Ensure final animated window size sync cannot be lost to backend resize throttling.
- Treat any pending approval/question/plan request as expanded immediately so no compact `236x52` resize is emitted for approval mode.
- Clamp expanded dimensions to at least 600x720 even if persisted config still contains old 420px values.
- Treat a session in `approval` state as approval focus mode even before a full `permission_request` payload arrives.

### Testing:
- `frontend`: `.\\node_modules\\.bin\\vite build` passed.

---

## [2026-05-03] - Approval/Question Focus Layout Fix

### Documentation Gate:
- User requested a new approval/question overlay layout behavior.
- Existing docs still defined a compact 420x600 expanded overlay, which is too small for approval/question flows when multiple sessions exist.
- Updated `DESIGN.md` and `docs/animation-design.md` before source edits to define 600x720 approval/question focus mode and single-main-scroll behavior.

### Planned correction:
- Approval/question/plan requests use a dedicated focus layout.
- Full session list is replaced by a light session context summary while approval is pending.
- ApprovalPanel keeps header/actions fixed and scrolls only the main content body, avoiding nested outer scrollbars.

### What was done:
- Added approval focus mode in `Overlay.tsx`: approval/question/plan requests render a dedicated 600x720 layout with a compact session context summary.
- Hid Settings/Activity footer controls while approval focus mode is active.
- Refactored `ApprovalPanel` so permission, question, and plan variants share a fixed header/footer with one scrollable body.
- Removed nested `max-height` scroll containers from action, question, and plan content; diff viewer keeps its own code-oriented scrolling.
- Updated frontend and backend overlay defaults to 600x720 expanded mode.
- Bumped config version to 2 and migrated existing version 1 overlay defaults from 420x600 to 600x720 when they still match old defaults.
- Updated testing docs and the overlay height regression script to expect the new approval height.

### Testing:
- `frontend`: `npm run build` passed after rerunning outside the sandbox because the first Vite config load failed with sandbox `spawn EPERM`.
- `src-tauri`: `cargo check` passed.

---

## [2026-05-03] - Task #38: 活动时间线

### What was done:
- 创建 `frontend/src/store/timeline.ts` Zustand store：
  - `timeRange` 状态支持 1h / 6h / 24h / all 筛选
  - `getTimeRangeMs` / `getTimeRangeOptions` 辅助函数
- 创建 `frontend/src/components/ActivityTimeline.tsx` 组件：
  - 从所有 sessions 的 toolHistory 聚合全局工具调用
  - 按时间戳倒序排列（最新在前）
  - 时间范围筛选下拉（1小时/6小时/24小时/全部）
  - 每个条目显示：时间、所属 session、工具详情
  - 点击条目通过 ToolExecutionDetail 展开查看完整信息
  - 导出功能：JSON 格式（含完整 input/output/error）或 Markdown 表格
  - 导出确认对话框，通过 Blob URL 触发下载
- 创建 `frontend/src/components/ActivityTimeline.css`：暗色主题、滚动条
- 集成到 `Overlay.tsx`：
  - 底部新增 "📊 Activity" 按钮
  - 点击切换 ActivityTimeline 视图
  - 与 Settings 面板互斥
  - ResizeObserver 自动适应高度

### Documentation:
- Requirement ref: task.json#38
- Design ref: architecture.md#Activity Timeline
- Docs updated: no - behavior defined in task description

### Testing:
- Frontend build passed (vite build)

### Notes:
- 时间线数据从 sessions store 的 toolHistory 派生，无需额外存储
- 导出文件命名格式：vibe-island-activity-<timestamp>.json/.md

---

## [2026-05-03] - Task #37: 工具执行详情

### What was done:
- 创建 `frontend/src/components/ToolExecutionDetail.tsx` 组件：
  - 每个工具执行条目可点击展开/折叠（带箭头动画）
  - 展开后显示：JSON 格式化的输入参数 (pre + syntax colors)
  - 输出摘要（截断到 2000 字符）
  - 错误信息红色高亮块（red background + border）
  - 执行 ID 元信息
- 创建 `frontend/src/components/ToolExecutionDetail.css`：状态颜色条、代码样式、滚动条
- 集成到 `SessionDetail.tsx`：替换静态 history items 为交互式 ToolExecutionDetail

### Documentation:
- Requirement ref: task.json#37
- Design ref: architecture.md#Tool Execution Detail
- Docs updated: no - behavior defined in task description

### Testing:
- Frontend build passed (vite build)

### Notes:
- 展开状态由 ToolExecutionDetail 内部管理，互不干扰
- 代码块使用等宽字体（Cascadia Code / Fira Code / Consolas fallback）
- 输入/输出 JSON 自动格式化（2-space indent）

---

## [2026-05-03] - Task #36: Session 持久化

### What was done:
- 创建 `src-tauri/src/session_store.rs` 模块：
  - `save_sessions(sessions_json)` - 保存前端传来的 JSON 到 `config_dir/vibe-island/sessions.json`
  - `load_sessions()` - 从文件加载 JSON，过期 7 天的 session 自动清理
  - 加载时将 session state 重置为 "done"（历史 sessions 不显示为活跃状态）
  - `get_session_path_info()` - 返回存储路径用于调试
- 添加 3 个 IPC 命令在 `commands.rs`：
  - `save_sessions` - 接收 JSON 字符串并持久化
  - `load_sessions` - 返回 JSON 字符串
  - `get_session_store_path` - 返回存储路径
- 在 `lib.rs` 注册 `session_store` 模块和 3 个命令
- 创建 `frontend/src/hooks/useSessionPersistence.ts`：
  - 启动时调用 `load_sessions` 恢复历史 sessions
  - 每 30 秒自动持久化当前 sessions
  - 页面卸载时（beforeunload）fire-and-forget 保存
  - 组件卸载时同步保存
  - `serializeSession` 过滤瞬态字段（currentTool 不持久化）
  - 跳过已存在的 session（避免覆盖实时 hook 数据）
- 在 `App.tsx` 集成 `useSessionPersistence` hook

### Documentation:
- Requirement ref: task.json#36
- Design ref: architecture.md#Session 持久化
- Docs updated: no - behavior defined in task description

### Testing:
- Frontend build passed (vite build)
- Rust backend: cargo check passed

### Notes:
- 存储路径: `%APPDATA%/vibe-island/sessions.json`
- 过期策略: 最后活动时间超过 7 天自动清理
- 恢复的 session state 设为 "done"（不作为活跃 session）
- 自动保存间隔 30 秒，最小化磁盘 I/O
- beforeunload 使用 fire-and-forget，可能不会在窗口关闭前完成（可接受的权衡）

---

## [2026-05-03] - Task #35: Session 分组与搜索

### What was done:
- 创建 `frontend/src/components/SessionList.tsx` 组件，替代 Overlay 中内联的 sessions 渲染：
  - 搜索框：按 session label 实时过滤（大小写不敏感）
  - 状态筛选下拉：All / Running / Thinking / Streaming / Approval / Error / Done / Idle
  - 排序切换按钮：按最近活动时间 / 按创建时间
  - 分组开关：按 cwd 自动分组，显示项目名和 session 数量
  - 分组折叠/展开：点击组头折叠，带动画过渡（Framer Motion AnimatePresence）
  - 空状态显示："No matching sessions"（有 sessions 但无匹配）/ "Waiting for agent sessions..."（无 sessions）
- 创建 `frontend/src/components/SessionList.css`：暗色主题工具栏、搜索框、分组头部、滚动条
- 集成到 `Overlay.tsx`：用 `<SessionList>` 替换原来的内联 sessions 映射和 motion.div
- 移除 Overlay.tsx 中的 `formatTime` 函数（已移至 SessionList）
- SessionList 使用 useMemo 优化过滤和分组计算

### Documentation:
- Requirement ref: task.json#35
- Design ref: architecture.md#Session 分组与搜索
- Docs updated: no - behavior defined in task description

### Testing:
- Frontend build passed (vite build)

### Notes:
- 搜索和过滤仅在前端执行，不修改 store
- 分组使用 Map 按 cwd 聚合，组件内部管理折叠状态
- 项目名提取函数 `extractProjectName` 取路径最后两段用于更清晰的上下文

---

## [2026-05-03] - Task #34: Session 详情面板

### What was done:
- 创建 `frontend/src/components/SessionDetail.tsx` 组件：
  - 显示 session 基本状态（label, state color dot）
  - 显示完整 cwd 路径、创建时间、最近活动时间
  - 显示 PID、Model、Source 等元信息
  - 显示当前工具卡片（工具名、输入预览、运行时长）
  - 显示最近错误信息（红色高亮）
  - 工具执行历史列表（最近 10 条，按时间倒序）
  - 每条历史记录显示：工具名、状态标签、耗时
  - 输入参数预览（截断 80 字符）
  - 历史条目左侧颜色条：success=绿, failed=红, running/pending=蓝
- 创建 `frontend/src/components/SessionDetail.css`：暗色主题、BEM 命名、滚动条样式
- 集成到 `Overlay.tsx`：
  - 新增 `viewingSessionId` 状态管理
  - 展开模式下点击 session → 显示 SessionDetail 替代 session 列表
  - 再次点击同一 session → 返回列表
  - Back 按钮返回 session 列表
  - 叠加层折叠时清空详情视图
  - session 被移除时自动清空详情视图
  - ResizeObserver 自动适应详情面板高度
- 所有动画使用 Framer Motion（slide in/out）

### Documentation:
- Requirement ref: architecture.md#Session detail panel
- Design ref: architecture.md#Session detail panel
- Docs updated: no - behavior defined in task description

### Testing:
- Frontend build passed (vite build)

### Notes:
- SessionDetail 使用独立滚动区域，不影响外层 Overlay 滚动
- 工具历史显示最近 10 条，store 中保留 20 条
- 审批面板在详情视图中依然显示在顶部

## [2026-05-03] - Task #42: 任务完成提示音功能

### What was done:
- 添加 rodio 音频依赖到 Cargo.toml（支持 WAV 格式）
- 创建 src-tauri/src/audio.rs 音频播放模块：
  - NotificationSound 枚举定义 12 种音效（None, Pop, Ping, Glass, Hero, Blow, Bottle, Frog, Funk, Morse, Purr, Tink）
  - 使用独立音频线程避免阻塞主线程
  - 支持 WAV 文件播放
  - 路径查找：开发模式 src-tauri/sounds/，生产模式 exe 目录/sounds/
- 实现 IPC 命令：
  - play_notification_sound(sound) - 播放指定提示音
  - get_notification_sounds() - 获取可用音效列表
- 前端 SettingsPanel.tsx 设置面板组件：
  - 音效选择按钮列表
  - 点击预览音效
  - 设置保存到 localStorage
- useAgentEvents.ts 监听 state_change 事件，done 状态时播放提示音
  - 1 秒防抖机制避免重复播放（React 严格模式）
- 生成 11 个 WAV 音效文件（正弦波 + 指数衰减包络）
- 集成到 Overlay UI，底部设置按钮切换

### Bug Fixes:
- 修复音效多次触发问题：移除后端 Stop hook 直接播放，统一由前端处理
- 修复路径查找问题：区分开发模式和生产模式
- 添加防抖机制：防止 React 严格模式导致的重复播放

### Documentation:
- Requirement ref: PRD FR-04
- Design ref: docs/hooks-setup.md
- Docs updated: yes

### Testing:
- Rust 后端编译通过 (cargo check)
- 前端构建通过 (npm run build)
- 音效播放测试通过

### Notes:
- 默认音效为 Hero
- 用户可在设置面板选择喜欢的提示音
- 选择 "None" 可禁用提示音
- 开发模式下音效文件在 src-tauri/sounds/

---

## [2026-05-02] - Task #41: 明显弹性灵动岛动画

### What was done:
- Follow-up bug record before source edits:
  - Bottom corners render as flat/straight edges in compact and expanded states.
  - Approval-triggered expansion can appear without visible spring animation.
  - Approval Approve/Reject clears the request but leaves the overlay expanded.
- Documentation Gate completed before source edits.
- Added Task #41 to `task.json`.
- Updated `docs/animation-design.md` with compact capsule dimensions, expanded dimensions, spring parameters, centered window resize behavior, and compact clickable interaction.
- Rebuilt `AnimatedOverlay` as the real Framer Motion shell for width, height, border radius, and subtle scale animation.
- Integrated `AnimatedOverlay` into `Overlay.tsx`, removed expand/collapse `set_window_size` jumps, and kept the compact capsule clickable.
- Replaced CSS `max-height` as the primary panel animation with Framer Motion content entry/exit and session item staggering.
- Extended `update_overlay_size` with `anchorCenter` support so window width changes around the current center point.
- Fixed follow-up issues by adding motion `clipPath` rounded clipping, deferring approval auto-expand by one animation frame, and collapsing the overlay when approval is cleared.
- Fixed remaining flat-bottom rendering by moving the black surface to `overlay__shell`, making bar/panel transparent, and adding WebView2-friendly paint containment and mask clipping.

### Documentation:
- Requirement ref: docs/animation-design.md#三动画参数配置
- Design ref: docs/animation-design.md#33-明显弹性灵动岛尺寸
- Docs updated: yes

### Testing:
- `src-tauri`: `cargo check` passed.
- `frontend`: `.\\node_modules\\.bin\\vite build` passed.
- `frontend`: targeted TypeScript check for `AnimatedOverlay.tsx`, `Overlay.tsx`, and `animation.ts` passed with `tsc --ignoreConfig`.
- `frontend`: `npm run build` is still blocked by existing unrelated test TypeScript errors (`vi` globals, unused test imports, and Session fixtures missing `createdAt`/`lastActivity`).

### Notes:
- User chose real window width/height spring animation and compact capsule click interaction.
- Existing untracked `docs/states-and-flows.md` was present before this task and is left untouched.

---

## [2026-05-02] - Task #31: 动画系统测试和文档

### What was done:
- 更新 AGENTS.md 添加动画系统章节
- 记录前端动画组件：StatusDot、Overlay
- 记录后端同步命令：update_overlay_size、set_window_interactive、set_rounded_corners
- 记录性能优化策略：IPC 节流、GPU 合成层、CSS 过渡

### Documentation:
- Requirement ref: architecture.md#Key Design Decisions
- Design ref: docs/animation-design.md
- Docs updated: yes

### Testing:
- 文档更新，无需运行时测试
- 前端和后端构建已通过

### Notes:
- 多显示器和高 DPI 场景已在 Task #7 中实现
- 前端动画测试已有 StatusDot.test.tsx
- 后端 IPC 测试已有 update_overlay_size 节流逻辑

---

## [2026-05-02] - Task #30: 动画性能优化

### What was done:
- 在 Overlay.css 添加 `will-change: transform` 和 `transform: translateZ(0)` 启用 GPU 加速
- 在 overlay__panel 添加 `will-change: max-height, opacity` 优化展开/收缩动画
- 在 overlay__session 添加 `will-change: background, transform` 优化悬停动画
- 在 StatusDot.css 添加 GPU 加速提示优化脉动动画
- IPC 节流已在 update_overlay_size 命令中实现（16ms 间隔）

### Documentation:
- Requirement ref: architecture.md#Key Design Decisions
- Design ref: docs/animation-design.md#六、性能优化
- Docs updated: no - behavior matches design

### Testing:
- Frontend build passed (vite build)

### Notes:
- `will-change` 提示浏览器预先创建合成层
- `translateZ(0)` 强制 GPU 渲染层
- 后端已有 16ms 节流，避免过频 Win32 调用

---

## [2026-05-02] - Task #29: Windows 11 圆角窗口

### What was done:
- 在 Cargo.toml 添加 Win32_Graphics_Dwm feature
- 在 overlay.rs 添加 Dwm import
- 实现 set_rounded_corners 函数，使用 DWMWA_WINDOW_CORNER_PREFERENCE
- 在 create_overlay_window 中调用圆角设置
- Windows 10 兼容性：API 不支持时静默失败，不影响功能

### Documentation:
- Requirement ref: architecture.md#Key Design Decisions
- Design ref: docs/animation-design.md#5.3 圆角设置（Windows 11）
- Docs updated: no - behavior matches design

### Testing:
- Rust 后端编译通过 (cargo check)

### Notes:
- 使用 DWMCWP_ROUND (值为 2) 设置圆角
- 仅 Windows 11 支持，Windows 10 会忽略

---

## [2026-05-02] - Task #27: 点击穿透自动切换

### What was done:
- 在 Overlay.tsx 添加 useEffect 监听 expanded 状态变化
- 调用 set_window_interactive 命令切换点击穿透模式
- 紧凑状态: 启用点击穿透 (interactive = false)
- 展开状态: 禁用点击穿透 (interactive = true)
- 审批请求时自动展开，强制进入交互模式

### Documentation:
- Requirement ref: architecture.md#Key Design Decisions
- Design ref: docs/animation-design.md#5.2 点击穿透切换
- Docs updated: no - existing behavior matches design

### Testing:
- Frontend build passed (vite build)

### Notes:
- 后端 set_window_interactive 命令已存在，无需修改
- 通过 WS_EX_TRANSPARENT 样式控制点击穿透

---

## [2026-05-02] - UI Width Overflow Follow-up: Long Session Labels

### What was done:
- Recorded implementation bug before source changes per Documentation Gate.
- Issue: long session labels derived from full paths can still overflow the fixed 420px status bar/session rows.
- Fixed backend session label extraction to handle Windows `\` path separators and trailing separators, so `D:\work\vibe-island\` becomes `vibe-island` instead of a full path label.
- Tightened overlay flex sizing so the status bar, session rows, current tool name, spinner, and status dot cannot expand past the fixed 420px overlay width.
- Added title attributes to truncated status bar and session list labels so the full label remains inspectable on hover.

### Documentation:
- Requirement ref: architecture.md#Key Constraints
- Design ref: DESIGN.md#前端模块
- Docs updated: no - fixed-width overlay and truncation behavior are already defined by existing implementation notes.

### Testing:
- `frontend`: `.\\node_modules\\.bin\\vite build` passed.
- `src-tauri`: `cargo test test_get_session_label -- --nocapture` passed using `CARGO_TARGET_DIR=target-codex-test` to avoid the running app's locked `target/debug/app.exe`.
- `frontend`: `npm run build` is still blocked by existing test TypeScript errors unrelated to this change (`vi` globals and test fixtures missing `createdAt`/`lastActivity`).

### Notes:
- Expected fix: status bar and session list labels remain inside the 420px overlay and truncate with ellipsis.

---

## [2026-05-02] - Documentation Sync: Current Code as Source of Truth

### What was done:
- Updated `architecture.md` to match registered Tauri commands, hook routes, Tauri events, session state model, hook config modes, and current module layout.
- Updated `DESIGN.md` to remove stale Mock/PTY descriptions and document the actual Hook server, hook_config, pipe server, process watcher, overlay, frontend store, and known implementation boundaries.
- Updated `README.md` to reflect automatic hook configuration, current state mapping, PermissionRequest approval flow, hook health, and SDK fallback.
- Updated `docs/hooks-setup.md` to match generated hook config and the current Claude Code PermissionRequest `hookSpecificOutput` response format.
- Updated `docs/testing-strategy.md` to describe only tests and dependencies currently present in the repository.
- Updated `docs/cc-experience-improvement.md` to separate completed hook/status work from remaining session and timeline work.
- Updated `AGENTS.md` overview and directory map for `hook_config.rs`, frontend config, and tests.

### Documentation:
- Requirement ref: user request to align requirements/docs with current implementation
- Design ref: architecture.md, DESIGN.md, docs/hooks-setup.md, docs/testing-strategy.md
- Docs updated: yes

### Testing:
- Documentation-only change; no runtime tests required.
- `task.json` parsed successfully with `ConvertFrom-Json`.
- `git diff --check` passed.
- `git diff --name-only` confirmed changes are limited to documentation/task tracking files.

### Notes:
- Current code still has implemented-but-unmounted frontend components (`HookConfigStatusPanel`, `ErrorLog`).
- Hook server implements `/hooks/post-tool-use-failure`, but auto hook configuration currently writes only the seven required hooks in `hook_config.rs`.

## [2026-05-02] - UI Width Overflow Fix: Hook Status and Approval Panel

### What was done:
- Recorded implementation bug before source changes per Documentation Gate.
- Issue: recent local UI/cache-related changes allowed Hook status content, long approval text, and diff preview rows to exceed the fixed overlay width.
- Correction: keep the existing fixed height behavior and constrain horizontal layout instead.

### Documentation:
- Requirement ref: architecture.md#Key Design Decisions
- Design ref: docs/animation-design.md#数据流
- Docs updated: no - existing overlay width is already fixed by the frontend layout and Tauri window sizing.

### Testing:
- `frontend`: `.\\node_modules\\.bin\\vite build` passed.

### Notes:
- Expected fix: overlay height remains on the existing fixed collapsed/expanded sizes; overflow is handled by truncation, wrapping, or internal horizontal scroll inside the constrained 420px width.

---

## [2026-05-02] - UI Corner Radius Fix: Approval Expanded Overlay

### What was done:
- Recorded implementation bug before source changes per Documentation Gate.
- Issue: when an approval request expands the overlay, the visible overlay surface can lose rounded corners because the bar and panel backgrounds are rendered as separate blocks without a shared clipping container.

### Documentation:
- Requirement ref: architecture.md#Key Design Decisions
- Design ref: docs/animation-design.md#四、前端实现
- Docs updated: no - existing visual design already expects a rounded overlay surface.

### Testing:
- `frontend`: `.\\node_modules\\.bin\\vite build` passed.

### Notes:
- Expected fix: keep the current fixed window height behavior; preserve rounded outer corners whenever approval content is visible.

---

## [2026-05-02] - Task #39 & #40: Hook 自动配置与清理

### What was done:
- 创建 src-tauri/src/hook_config.rs 模块
- 实现 check_hook_config 命令 - 检查 settings.json 中是否包含 Vibe Island hooks
- 实现 install_hooks 命令 - 读取现有 settings.json，合并 Vibe Island hooks，创建备份
- 实现 uninstall_hooks 命令 - 从 settings.json 中移除 Vibe Island hooks
- 实现 get_hook_config_status 命令 - 返回配置状态
- 实现 set_hook_config_mode 命令 - 设置配置模式
- 在 lib.rs setup 中调用 auto_configure_hooks，启动时自动配置
- 在 quit 事件中调用 auto_cleanup_hooks，退出时自动清理（仅在 auto-cleanup 模式）
- 添加 dirs 依赖用于获取用户主目录
- 创建前端 HookConfigStatus 组件，支持配置状态显示和模式切换

### Documentation:
- Requirement ref: architecture.md#IPC Commands
- Design ref: docs/hooks-setup.md#Automatic Hook Configuration
- Docs updated: yes

### Testing:
- Rust 后端编译通过 (cargo check)
- 前端构建通过 (vite build)

### Notes:
- 支持三种配置模式：auto（默认，启动配置但不清理）、auto-cleanup（启动配置退出清理）、manual（手动管理）
- 备份文件位置：~/.claude/settings.json.vibe-island-backup
- 自动配置仅在 hooks 未配置时执行
- 退出清理仅在 auto-cleanup 模式下执行
- **重要安全特性**：merge_hooks 不会覆盖用户已有的非 Vibe Island hook 配置
  - 如果用户已配置同名 hook（如 SessionStart）指向其他服务，会跳过不覆盖
  - 只更新已指向 Vibe Island 的 hook
  - 添加缺失的 hook

### What was done:
- 添加 Task #39: Hook 自动配置 - 启动检查与配置
- 添加 Task #40: Hook 自动清理 - 退出时移除配置
- 更新 docs/hooks-setup.md 添加 Automatic Hook Configuration 章节
- 更新 architecture.md IPC Commands 表格添加新命令

### Documentation:
- Requirement ref: architecture.md#IPC Commands
- Design ref: docs/hooks-setup.md#Automatic Hook Configuration
- Docs updated: yes

### Notes:
- 用户请求：软件启动时检查 Hook 配置是否存在，自动进行配置；软件退出时自动清理之前安装的 Hook
- 支持三种配置模式：auto（默认）、auto-cleanup、manual
- 配置前创建备份文件 ~/.claude/settings.json.vibe-island-backup

---

## [2026-05-02] - PermissionRequest Hook 响应格式修复

### What was done:
- 修复 PermissionRequest hook 响应格式错误
- 原格式: `{"behavior":"allow"}` (错误)
- 正确格式: `{"hookSpecificOutput":{"hookEventName":"PermissionRequest","decision":{"behavior":"allow"}}}`
- 参考官方文档: https://code.claude.com/docs/en/hooks

### Documentation:
- Requirement ref: architecture.md#Hook Server
- Design ref: docs/hooks-setup.md
- Docs updated: yes

### Testing:
- 用户点击 Approve 后命令继续执行 ✓
- 审批流程完整工作 ✓

### Notes:
- 这是导致审批对话框关闭但命令不继续执行的根本原因
- Claude Code 期望特定的 JSON 响应格式，格式错误会被静默忽略

---

## [2026-05-02] - 测试系统重构

### What was done:
- 创建完整的测试策略文档 docs/testing-strategy.md
- 前端测试配置：Vitest + Testing Library
- 后端测试配置：Rust 内置测试 + tokio-test
- 实现 Store 测试 (sessions.test.ts) - 21 个测试用例
- 实现 Hook 测试 (useAgentEvents.test.ts) - 18 个测试用例
- 实现 StatusDot 组件测试 - 9 个测试用例
- 实现 ApprovalPanel 组件测试 - 9 个测试用例
- 后端 hook_server.rs 内联测试 - 27 个测试用例
- 后端集成测试 hook_server_integration.rs - 12 个测试用例

### Documentation:
- Requirement ref: architecture.md#Testing
- Design ref: docs/testing-strategy.md
- Docs updated: yes

### Testing:
- 前端测试: 56 passed (vitest run)
- 后端测试: 39 passed (cargo test)

### Notes:
- 测试覆盖核心业务逻辑，不依赖 UI 截图
- Mock Tauri API 实现前端隔离测试
- 测试可在 CI/CD 中自动运行

---

## [2026-05-02] - Task #33: Hook 事件增强

### What was done:
- 扩展 AgentState 枚举: thinking, streaming, error
- 新增 `/hooks/post-tool-use-failure` 端点处理工具执行失败
- PreToolUse 现在触发 `thinking` 状态（而非 `running`）
- PostToolUse 触发 `streaming` 状态
- PostToolUseFailure 触发 `error` 状态
- 前端 StatusDot 组件支持新状态的动画：
  - thinking: 紫色脉动缩放动画
  - running: 蓝色透明度脉动
  - streaming: 青色快速透明度脉动
  - error: 红色静态
- 工具执行历史记录添加 duration 和 error 字段

### Documentation:
- Requirement ref: architecture.md#Session States
- Design ref: architecture.md#HTTP Hook Endpoints
- Docs updated: yes

### Testing:
- Rust 后端编译通过 (cargo check)
- 前端构建通过 (npm run build)

### Notes:
- 新增 tool_error 事件类型
- 工具执行耗时统计通过 duration_ms 字段传递

---

## [2026-04-28] - Task #32: Hook Server 健康监控

### What was done:
- 新增 `HookHealthStatus` 结构体（status, uptime_secs, request_count, pending_approvals, last_heartbeat）
- 新增 `/hooks/health` GET 端点
- 更新 `HookServerState` 结构体添加 start_time, request_count, last_heartbeat
- 前端 HookStatus 组件显示实时统计信息

### Documentation:
- Requirement ref: architecture.md#Hook Server
- Design ref: DESIGN.md#Hook Integration
- Docs updated: yes

### Testing:
- Windows 环境编译测试通过
- 前端显示效果验证通过

### Notes:
- 实现了心跳检测机制
- 添加了 get_hook_health、get_hook_errors、clear_hook_errors IPC 命令

---

## [2026-04-28] - Tasks #24, #25, #26, #28: 动画系统基础

### What was done:
- Task 24: 添加 Framer Motion 动画依赖
- Task 25: 实现 AnimatedOverlay 组件
- Task 26: 实现后端 update_overlay_size 命令
- Task 28: 实现 StatusDot 状态动画

### Documentation:
- Requirement ref: docs/animation-design.md
- Design ref: docs/research-dynamic-island-animation.md
- Docs updated: yes

### Testing:
- 动画流畅性测试通过
- IPC 节流验证正确

### Notes:
- 弹簧动画参数：stiffness=300, damping=25, mass=0.8
- IPC 节流间隔：16ms

---

## [2026-04-26] - Task #10: UI Polish and Production Readiness

### What was done:
- Panel expand/collapse animation (300ms ease-out)
- State change color transitions (200ms)
- Loading spinner and error display with dismiss
- Hover effects on sessions with scale transform

### Documentation:
- Requirement ref: architecture.md#Frontend Component Map
- Design ref: DESIGN.md#UI Design
- Docs updated: yes

### Testing:
- Production build passes
- Windows 10/11 compatibility verified

---

## [2026-04-26] - Batch 4: Tasks #5, #7, #8, #9 (Parallel)

### Task #5: Frontend - Approval Panel Component
- Files: ApprovalPanel.tsx, ApprovalPanel.css, sessions.ts, commands.rs, events.rs
- Implementation: Displays session label, action description, risk level
- Keyboard shortcuts: Enter (approve), Escape (reject)

### Task #7: DPI & Multi-Monitor Support
- Files: overlay.rs, commands.rs, lib.rs
- Implementation: GetDpiForWindow and GetScaleFactorForMonitor wrappers
- Per-monitor DPI awareness v2

### Task #8: Agent SDK - Node.js
- Files: agent-sdk/node/
- Implementation: Named Pipe client with auto-reconnect

### Task #9: Agent SDK - Python
- Files: agent-sdk/python/
- Implementation: Named Pipe client using pywin32

### Documentation:
- Requirement ref: architecture.md#Agent SDK
- Design ref: DESIGN.md#SDK Design
- Docs updated: yes

### Testing:
- All verifiers: PASS

---

## [2026-04-25] - Task #4: Process Watcher

### What was done:
- Create process_watcher.rs with CreateToolhelp32Snapshot
- Detect known agents: claude.exe, codex.exe, aider.exe, cursor.exe, copilot-agent.exe
- Thread-safe state with parking_lot::Mutex and AtomicBool

### Documentation:
- Requirement ref: architecture.md#Process Watcher
- Design ref: DESIGN.md#Process Detection
- Docs updated: yes

### Testing:
- Verifier: PASS

---

## [2026-04-25] - Tasks #3 and #6 (Parallel)

### Task #3: Named Pipe Server Foundation
- Files: pipe_server.rs, lib.rs, commands.rs
- Named Pipe: `\\.\pipe\VibeIsland`
- tokio::spawn for concurrent client connections

### Task #6: Window Focus Management
- Files: window_focus.rs, commands.rs, Overlay.tsx
- Focus fallback chain: SetForegroundWindow → AttachThreadInput → FlashWindowEx → ShowWindow

### Documentation:
- Requirement ref: architecture.md#Module Map
- Design ref: DESIGN.md#Window Management
- Docs updated: yes

### Testing:
- All verifiers: PASS

---

## [2026-04-25] - Task #1: Core Infrastructure

### What was done:
- System tray with Show/Hide and Quit menu items
- Event types: SessionStart, SessionEnd, StateChange, ApprovalRequest
- Frontend hook listens to all events and dispatches to Zustand store

### Documentation:
- Requirement ref: architecture.md#Core Infrastructure
- Design ref: DESIGN.md#System Tray
- Docs updated: yes

### Testing:
- Verifier: PASS (compilation verified)

---

## Task Changes Log

### 2026-04-25 - Task Restructuring

**Changes based on code review feedback:**

1. **Merged Task 1 + 2** → New Task 1 "Core Infrastructure: System Tray + Event Bridge"
2. **Task 2 (Mock Mode) priority raised** → high
3. **New Task 7: DPI & Multi-Monitor Support**
4. **Task 8/9 (Agent SDK)** - Added distribution/publishing steps

---

## Execution Order

```
[Task 1] → [Task 2] → [Task 3 + 5] → [Task 4 + 6 + 7] → [Task 8 + 9] → [Task 10]
  骨架        Mock        通道+UI         后端功能           SDK           收尾
```

---

## [2026-05-02] - Task #27: 点击穿透自动切换

### What was done:
- 在 Overlay.tsx 添加 useEffect 监听 expanded 状态变化
- 调用 set_window_interactive 命令切换点击穿透模式
- 紧凑状态: 启用点击穿透 (interactive = false)
- 展开状态: 禁用点击穿透 (interactive = true)
- 审批请求时自动展开，强制进入交互模式

### Documentation:
- Requirement ref: architecture.md#Key Design Decisions
- Design ref: docs/animation-design.md#5.2 点击穿透切换
- Docs updated: no - existing behavior matches design

### Testing:
- Frontend build passed (vite build)

### Notes:
- 后端 set_window_interactive 命令已存在，无需修改
- 通过 WS_EX_TRANSPARENT 样式控制点击穿透

---

## Pending Tasks

All tasks completed.
